<?php

    /*function par($inicial, $final) {

        $par = (string) null;

        for ($i = $inicial ; $i <= $final ; $i++) {
            
            if ($i % 2 == 0) {
                $par = $par . $i . "<br>";
            }
        
        } 
    
        return $par;
    } 

    function impar($inicial, $final) {

        $impar = (string) null;

        for ($i = $inicial ; $i <= $final ; $i++) {
            
            if ($i % 2 == 1) {
                $impar = $impar . $i . "<br>";
            }
        
        } 
        
        return $impar;
    }

    function quantPar($inicial, $final) {

        $quantPar = (int) null;
        $quantImpar = (int) null;

        for ($i = $inicial ; $i <= $final ; $i++) {

            $par = (int) null;
            $impar = (int) null;
            
            if ($i % 2 == 1) {
                $par = $par . $i . "<br>";
                $quantPar++;
            } else {
                $impar = $impar . $i . "<br>";
                $quantImpar++;
            }
        }

        return $quantPar++;
    }

    function quantImpar($inicial, $final) {

        $quantImpar = (int) null;
        $quantPar = (int) null;

        for ($i = $inicial ; $i <= $final ; $i++) {
            
            $impar = (int) null;
            $par = (int) null;

            if ($i % 2 == 1) {
                $impar = $impar . $i . "<br>";
                $quantPar++;
            } else {
                $par = $par . $i . "<br>";
                $quantImpar++;
            }
        }

        return $quantImpar++;
    }*/

?>